@model List<AcunMedyaPortfolio.Models.TblProfile>
@foreach (var item in Model)
{
    <div>
        <ul class="about-info mt-4 px-md-0 px-2">
            <li class="d-flex"><span>Ad-Soyad:</span> <span>@item.Name</span></li>
            <li class="d-flex"><span>Doğum Tarihi:</span> <span>@item.Birthday</span></li>
            <li class="d-flex"><span>Adres:</span> <span>@item.Adress</span></li>
            <li class="d-flex"><span>Email:</span> <span>@item.Email</span></li>
            <li class="d-flex"><span>Telefon: </span> <span>@item.Telephone</span></li>
        </ul>

    </div>
}